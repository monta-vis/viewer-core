{
  "name": "montavis-viewer",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build:core": "npm run build -w packages/viewer-core",
    "build:app": "npm run build -w packages/viewer-app",
    "dev:core": "npm run dev -w packages/viewer-core",
    "dev:app": "npm run dev -w packages/viewer-app",
    "dev:electron": "npm run dev:electron -w packages/viewer-app",
    "test": "npm run test -w packages/viewer-core",
    "typecheck": "tsc -b"
  }
}
